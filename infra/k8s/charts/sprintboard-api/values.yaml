image:
  repo: ghcr.io/Rohan-2110-git/sprintboard-api
  tag: "latest"

replicaCount: 1

env:
  DATABASE_URL: "sqlite:///./sprintboard.db"
  PORT: "8000"

service:
  port: 8000

ingress:
  enabled: true
  host: api.yourdomain.com

# leave empty for public registries
imagePullSecret: ""
