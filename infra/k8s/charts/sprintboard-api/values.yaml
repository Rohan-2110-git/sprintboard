image:
  repo: 590183888289.dkr.ecr.ap-south-1.amazonaws.com/sprintboard-api
  tag: "latest"

replicaCount: 1

env:
  DATABASE_URL: "sqlite:///./sprintboard.db"
  PORT: "8000"

service:
  port: 8000

ingress:
  enabled: true
  host: api.yourdomain.com

imagePullSecret: ecr-creds
